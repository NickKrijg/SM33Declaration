<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <android.support.v7.widget.CardView
        android:id="@+id/card_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:layout_margin="@dimen/card_margin"
        android:elevation="3dp"
        card_view:cardCornerRadius="@dimen/card_album_radius">

        <android.support.constraint.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            >


            <TextView
                android:id="@+id/title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingLeft="@dimen/declaration_title"
                android:paddingRight="@dimen/declaration_title"
                android:paddingTop="@dimen/card_margin"
                android:textColor="@color/declaration_title"
                android:textSize="@dimen/declaration_title"
                card_view:layout_constraintStart_toEndOf="@+id/overflow"/>

            <TextView
                android:id="@+id/description"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/title"
                android:paddingBottom="@dimen/declaration_description_padding_bottom"
                android:paddingLeft="@dimen/declaration_title"
                android:paddingRight="@dimen/declaration_title"
                android:textSize="@dimen/declaration_description"
                card_view:layout_constraintTop_toBottomOf="@+id/title"/>


            <ImageView
                android:id="@+id/overflow"
                android:layout_width="@dimen/ic_album_overflow_width"
                android:layout_height="match_parent"
                android:src="@color/statusAccepted"
                card_view:layout_constraintStart_toStartOf="parent"/>

            <TextView
                android:id="@+id/price"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:paddingRight="@dimen/card_margin"
                android:textSize="@dimen/declaration_title"
                card_view:layout_constraintEnd_toEndOf="parent"
                android:layout_marginRight="5dp"
                />


        </android.support.constraint.ConstraintLayout>

    </android.support.v7.widget.CardView>

</LinearLayout>